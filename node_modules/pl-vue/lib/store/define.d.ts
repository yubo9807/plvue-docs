import { AnyObj } from "../utils";
type State = Record<string, any>;
type Actions = Record<string, Function>;
export declare const states: AnyObj;
/**
 * 定义 Store
 * @param state
 * @param actions
 * @returns
 */
export declare function defineStore<S extends State, A extends Actions>(option: {
    id?: string;
    state: S;
    actions: A;
}): () => S & A;
export {};
