type Obj = Record<string, any>;
/**
 * 创建一个 Store
 * @param state
 * @param actions
 * @returns
 */
export declare function createStore<S extends Obj, A extends Obj>(state: S, actions: A): () => S & A;
export {};
