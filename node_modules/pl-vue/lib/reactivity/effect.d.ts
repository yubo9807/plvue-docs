import { RefImpl } from "./ref";
export declare class ReactiveEffect<T> {
    fn: () => T;
    computed: RefImpl<T>;
    active: boolean;
    constructor(fn: () => T);
    /**
     * 任务调度
     */
    scheduler(): void;
    /**
     * 执行任务
     * @returns
     */
    run(): T;
    /**
     * 停止任务调度
     */
    stop(): void;
}
