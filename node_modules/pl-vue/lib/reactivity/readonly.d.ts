import { AnyObj } from "../utils";
/**
 * 将对象转为只读
 * @param target
 * @returns
 */
export declare function readonly(target: AnyObj): AnyObj;
/**
 * 检测是否为 readonly 对象
 * @param proxy
 * @returns
 */
export declare function isReadonly(proxy: AnyObj): any;
