import { Children, Tree } from "./type";
export type StaticOption = {};
export declare class Static {
    constructor();
    /**
     * 树形结构拦截
     * @param tree
     */
    intercept<T>(tree: T): T;
    /**
     * 服务端渲染函数
     * @param tree
     * @returns
     */
    renderToString(tree: Tree): string;
    /**
     * 创建 innerHTML，用于服务端渲染
     * @param tree
     */
    createHTML(tree: Tree): any;
    /**
     * 创建 innerHTML 片段
     * @param children
     * @returns
     */
    createHTMLFragment(children: Children): string;
}
