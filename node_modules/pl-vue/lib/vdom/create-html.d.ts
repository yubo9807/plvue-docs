import { Attrs, Children, Tag } from "./type";
/**
 * 创建 innerHTML，用于服务端渲染
 * @param tag
 * @param attrs
 * @param children
 */
export declare function createHTML(tag: Tag, attrs?: Attrs, children?: Children): string;
/**
 * 创建 innerHTML 片段
 * @param children
 * @returns
 */
export declare function createHTMLFragment(children: Children): string;
