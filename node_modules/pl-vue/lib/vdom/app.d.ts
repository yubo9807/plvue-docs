import { Component } from "./type";
import { Structure, StructureOption } from "./create-element";
type Plugin = {
    install: (app: App) => void;
};
export declare class App extends Structure {
    #private;
    [k: string]: any;
    constructor(option: StructureOption);
    use(plugin: Plugin): this;
    version: string;
    /**
     * 数据拦截
     */
    intercept(tree: any): any;
    /**
     * 注册全局组件
     * @param name
     * @param Comp
     */
    component(name: string, Comp: Component): this;
}
/**
 * 创建应用
 * @param option
 * @returns
 */
export declare function createApp(): App;
export {};
