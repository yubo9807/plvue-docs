import { Component, IntailOption } from "./type";
import { Element } from "./create-element";
type Plugin = {
    install: (app: App) => void;
};
export declare class App extends Element {
    #private;
    [k: string]: any;
    constructor(config: IntailOption);
    use(plugin: Plugin): this;
    version: string;
    /**
     * 数据拦截
     */
    intercept(tree: any): any;
    /**
     * 注册全局组件
     * @param name
     * @param Comp
     */
    component(name: string, Comp: Component): this;
}
/**
 * 创建应用
 * @param option
 * @returns
 */
export declare function createApp(option?: IntailOption): App;
export {};
