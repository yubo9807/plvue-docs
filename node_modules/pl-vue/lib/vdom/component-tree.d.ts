import { Attrs, Children, Component } from './type';
type CompTree = {
    comp: Component;
    props: Attrs;
    children?: Children;
};
export declare const compTreeMap: WeakMap<Component, CompTree[]>;
/**
 * 过滤掉元素，对组件进行收集
 * @param children
 * @param collect  递归参数，无需传递
 * @returns
 */
export declare function filterElement(children: Children, collect?: CompTree[]): CompTree[];
/**
 * 获取一个组件下所有的子组件
 * @param comp    组件
 * @param collect 递归参数，无需传递
 * @returns
 */
export declare function getSubComponent(comp: Component, collect?: CompTree[]): CompTree[];
/**
 * 获取组件树型结构
 * @param comp 组件
 */
export declare function getComponentTree(comp: Component): CompTree[];
export {};
