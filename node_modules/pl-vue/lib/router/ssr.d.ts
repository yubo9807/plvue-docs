import { Children, PropsType } from '../vdom';
type Props = PropsType<{
    children?: Children;
}>;
export declare function Helmet(props: Props): {
    tag: import("../vdom").Tag;
    attrs: import("../utils").AnyObj;
    children: Children;
};
/**
 * 生成 SSR 页面
 * @param App      根组件
 * @param url      req.url
 * @param template html 模版
 * @returns
 */
export declare function ssrOutlet(content: string, url: string, template: string): Promise<unknown>;
export {};
