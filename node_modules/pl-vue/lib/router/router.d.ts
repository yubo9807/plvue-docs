import { Component, PropsType, BaseComponent, Children } from "../vdom";
type BrowserRouterProps = PropsType<{
    loading?: BaseComponent;
    notFound?: BaseComponent;
    prefix?: string;
    children?: Children;
}>;
export declare const stack: string[];
type StaticRouterProps = PropsType<{
    notFound?: Component;
    children?: Children;
}>;
/**
 * BrowserRouter & StaticRouter
 * @param props
 * @returns
 */
export declare function Router(props: BrowserRouterProps & StaticRouterProps): any;
export {};
