import { AnyObj } from "../utils";
import { Component, PropsType, Tree } from "../vdom";
import { BeforeEnter } from "./type";
type RouteProps = PropsType<{
    path: string;
    component: Component | (() => Promise<Component>);
    exact?: boolean;
    beforeEnter?: BeforeEnter;
    meta?: AnyObj;
}>;
export declare function Route(props: RouteProps): void;
export type RouteItem = Tree & {
    attrs: RouteProps;
};
/**
 * 查找路由
 * @param routes
 * @param pathname
 * @returns
 */
export declare function queryRoute(routes: RouteItem[], pathname: string): {
    path: string;
    component: Function | (() => Promise<Function>);
    meta: AnyObj;
    beforeEnter: BeforeEnter;
};
export {};
